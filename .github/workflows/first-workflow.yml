name: First Workflow
on: [push]

jobs:
    run-shell-commands:
        runs-on: ubuntu:18.04
        steps:
          - name: echo a String
            run: echo "Hello World"
          - name: Multiline Commands
            run: |
              node -v
              npm -v